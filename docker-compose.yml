# Removido. O deploy é feito via Dockerfile no Dokploy:
#   - Postgres é serviço separado (gerenciado pelo Dokploy ou externo)
#   - Backend roda só com `docker build` deste diretório.
# Mantido vazio porque o workspace não permite remoção do arquivo.

services: {}
